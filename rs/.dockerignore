# Build artifacts — large and rebuilt inside Docker anyway
target/

# Git metadata
.git/
.gitignore

# Log files produced at runtime
*.log
moq-keycast.log

# Editor / IDE artefacts
.vscode/
.idea/
*.swp
